// Public configuration only. Never add a Google client secret here.
window.GOOGLE_AUTH_CONFIG = Object.freeze({
  clientId: "REPLACE_WITH_GOOGLE_WEB_CLIENT_ID.apps.googleusercontent.com",
  allowedOpenerOrigins: Object.freeze([
    "https://REPLACE_WITH_ITCH_IFRAME_ORIGIN"
  ])
});
